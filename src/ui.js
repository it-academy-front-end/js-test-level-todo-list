/**
 * Se encarga de toda la manipulación del DOM.
 * Debes implementar la lógica para renderizar la lista de tareas.
 */

export const renderTodos = (todos, listElement) => {
  // renderiza los `<li>` dentro del `<ul>`.
}