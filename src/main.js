import { addTodo, getTodos } from './todos.js';
import { renderTodos } from './ui.js';

/**
 * Punto de entrada de la aplicación.
 * Conecta los eventos del usuario (clicks, etc.) con la lógica de la aplicación.
 */

// Selecciona los elementos del DOM necesarios y añade los event listeners.
document.addEventListener('DOMContentLoaded', () => {
  // Aquí es un buen lugar para empezar.
});